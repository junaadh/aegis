{
  "name": "aegis-sdk",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "typecheck": "bun run --filter @aegis/core typecheck && bun run --filter @aegis/server typecheck && bun run --filter @aegis/react typecheck",
    "build": "bun run --filter @aegis/core build && bun run --filter @aegis/server build && bun run --filter @aegis/react build"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}

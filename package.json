{
  "name": "aegis",
  "private": true,
  "workspaces": [
    "sdk/packages/*"
  ],
  "scripts": {
    "build": "bun run --cwd sdk build",
    "typecheck": "bun run --cwd sdk typecheck",
    "test": "bun run --cwd sdk test"
  }
}
